{{!< default}}
<section class="wrap">

  <h1 class="page-title">Books Blog Archive</h1>

  <div class="tag-menu">
    <ul>
      <li><a href="/blog">All</a></li>
      <li><a href="/web"><i class="fa fa-tag"></i> Web</a></li>
      <li><a href="/books" class="active-tag"><i class="fa fa-tag"></i> Books</a></li>
    </ul>
  </div>

  {{#post}}
    {{content}}
  {{/post}}

  {{#get "posts" limit="all" include="author,tags" filter="tag:books" as |posts pages|}}

    <p>A collection of <strong>{{pages.total}}</strong> post(s).</p>

    <hr>

    {{#foreach posts}}
      <article class="{{post_class}}">
        <section class="list-content">
          <header class="cover post post-head">
            <h1 class="cover-heading post-title"><a class="post-title-link" href="{{url}}">{{{title}}}</a></h1>
            <section class="post-meta">
              Posted on <time datetime="{{date format="YYYY-MM-DD"}}">{{date format='DD MMM YYYY'}}</time>
              {{#if tags}}in {{tags separator=", "}}{{/if}}
            </section>
          </header>
          <section class="post-excerpt">
            <div class="cover post post-body">
              <p>{{excerpt words="30"}}<a class="read-more" href="{{url}}">...</a></p>
            </div>
          </section>
        </section>
        <hr>
      </article>
    {{/foreach}}

  {{/get}}

</section>
